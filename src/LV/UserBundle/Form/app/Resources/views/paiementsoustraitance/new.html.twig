{% extends 'base.html.twig' %}
{% block menu %}
<div class="subnavbar">
  <div class="subnavbar-inner">
    <div class="container">
      <ul class="mainnav">
        <li><a href="{{path('accueil')}}"><i class="icon-dashboard"></i><span>Tableau de Bord</span> </a> </li>
        <li><a href="{{path('voiture_index')}}"><i class="fa fa-automobile"></i><span>Vehicule</span> </a> </li>
        <li><a href="{{path('client_index')}}"><i class="shortcut-icon icon-user"></i><span>Client</span> </a></li>
        <li><a href="{{path('reservation_index')}}"><i class="shortcut-icon icon-save"></i><span>Reservation</span> </a> </li>
        <li><a href="{{path('csr_index')}}"><i class="shortcut-icon icon-edit"></i><span>Contrat</span> </a> </li>
        <li><a href="{{path('paiement_index')}}"><i class="shortcut-icon icon-random"></i><span>Paiement</span> </a> </li>
        <li><a href="{{path('facture_index')}}"><i class="shortcut-icon icon-list-alt"></i><span>Facture</span> </a> </li>
      </ul>
    </div>
  </div>
</div>
{% endblock %}
{% block body %}<div class="main-inner">
    <div class="container">
      <div class="row">
        <div class="span12">

        
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <h1><center>Ajouter paiement d'une Soutraitance</center></h1>
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('accueil') }}"><i class="fa fa-dashboard"></i> Tableau de Bord</a></li>
        <li  class="breadcrumb-item active"> /<a href="{{ path('soustraitance_detail', { 'id': stcid }) }}"> Paiement Soustraitance</a></li>
        <li  class="breadcrumb-item active"> / Ajouter Paiement Soustraitance</li>
      </ol>
    </section>

    <!-- Main content -->
     <section class="content" style="padding-left:50px;padding-right:50px;">
      <div class="row">
        <div class="col-md-12">
              <!-- TABLE: LATEST ORDERS -->
              <div class="box box-info">
                <div class="box-header with-border">
                  <h3 class="box-title"><center>Ajouter Paiement d'une Soustraitance</center></h3>
                </div><br><br>
                <div class="box-body">
<div class="row">
<div class="span4">&nbsp;
</div>
<div class="span4">
    {{ form_start(form) }}
        <div class="row">
           <div class="col-sm-4">{{ form_label(form.date) }}</div>
           {{ form_errors(form.date) }}
           <div class="col-sm-8">{{ form_widget(form.date,{'attr': {'readonly':true}}) }}</div>
        </div>
        <div class="row">
           <div class="col-sm-4">{{ form_label(form.montant) }}</div>
           {{ form_errors(form.montant) }}
           <div class="col-sm-8">{{ form_widget(form.montant,{'attr': {'readonly':true}}) }}</div>
        </div>
        <div class="row" style="display:none;">
           <div class="col-sm-4">{{ form_label(form.refSoustraitance) }}</div>
           {{ form_errors(form.refSoustraitance) }}
           <div class="col-sm-8">{{ form_widget(form.refSoustraitance) }}</div>
        </div>
		<div class="row" align="right">
           <input type="submit" class="btn btn-success" value="Ajouter" />
		</div>
    {{ form_end(form) }}
</div>
</div>
  </div>
  </div>
  </div>
  </div>
  </section>
  </div>
  </div>
  </div>
  </div>
  </div>
  </div>
{% endblock %}
